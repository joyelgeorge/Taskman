# Collects declared observation sources, rolls up the day, prunes raw rows.
name: cron · data-collect

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:
    inputs:
      force:
        description: Run even if this slot already ran (manual runs only).
        type: boolean
        default: true

concurrency:
  group: cron-data-collect
  cancel-in-progress: false

jobs:
  run:
    uses: ./.github/workflows/_cron.yml
    with:
      name: data-collect
      # Only ever true for a manual dispatch; a scheduled run leaves it false so
      # slot idempotency still governs bursts of deliveries.
      force: ${{ github.event_name == 'workflow_dispatch' && inputs.force || false }}
    secrets:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      STRIPE_API_KEY: ${{ secrets.STRIPE_API_KEY }}
